node_modules
dist
coverage
.DS_Store
*.local
.vite

# Yarn 4 (nodeLinker: node-modules) — keep only the committed tooling
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnp.*
.yarn/install-state.gz
